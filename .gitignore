# Dependencies
node_modules/

# TLS certificates
certs/
.pnp
.pnp.js
.yarn/install-state.gz

# Next.js build output
.next/
out/
dist/

# Production build
build/

# Environment files (NEVER commit real secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Keep .env.example — it documents required variables without secrets
!.env.example

# Prisma generated files (regenerated via `prisma generate`)
# Keep schema.prisma and migrations — commit those
prisma/generated/

# Local SQLite (if ever used for local dev)
*.db
*.db-journal
*.sqlite
*.sqlite3

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/
.nyc_output/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE / Editor files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*.swo
*~
.project
.settings/
.classpath

# Local file uploads (development)
public/uploads/

# AWS / Cloud credentials
.aws/
*.pem
*.key
*.cert
*.p12
*.pfx

# Vercel
.vercel

# Package manager locks — commit only ONE of these
# (this project uses pnpm — commit pnpm-lock.yaml)
yarn.lock
package-lock.json
bun.lockb

# Misc
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
*.tgz
.node_repl_history
